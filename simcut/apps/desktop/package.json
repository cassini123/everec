{
  "name": "simcut-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "ui:dev": "npm run dev --prefix ui",
    "ui:build": "npm run build --prefix ui"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
